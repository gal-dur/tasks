{
  "name": "tasks-web",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.tsx --bundle --outfile=dist/board.js --jsx=automatic --minify --define:process.env.NODE_ENV='\"production\"' && cp src/index.html src/board.css dist/",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-markdown": "^10.1.0",
    "remark-gfm": "^4.0.1"
  },
  "devDependencies": {
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0"
  }
}
